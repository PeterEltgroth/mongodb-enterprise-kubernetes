apiVersion: mongodb.com/v1
kind: MongoDBOpsManager
metadata:
  name: my-{{ .Release.Name }}-ops-manager
spec:
  replicas: 1
  version: 4.2.12
  adminCredentials: {{ .Release.Name }}-ops-manager-secret
  externalConnectivity:
    type: LoadBalancer
  applicationDatabase:
    members: 3
    version: 4.2.2-ent
    persistent: true
